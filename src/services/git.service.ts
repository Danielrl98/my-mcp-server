import { execSync } from "child_process";

export function gitBackup() {
  // try {
  //   execSync("git add .");
  //   execSync(`git commit -m "backup before MCP edit"`);
  // } catch {
  //   console.log("Git backup skipped.");
  // }
}
